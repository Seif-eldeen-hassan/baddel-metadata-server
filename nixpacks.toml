[phases.setup]
nixPkgs = ["nodejs_18", "python3", "python3Packages.pip", "gcc", "python3-dev"]

[phases.install]
cmds = [
    "npm install --legacy-peer-deps", 
    "python3 -m pip install --upgrade pip",
    "pip3 install -r requirements.txt"
]

[start]
cmd = "npm start"