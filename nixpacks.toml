[phases.setup]
nixPkgs = ["nodejs_22", "python3", "python3Packages.pip", "gcc"]

[phases.install]
cmds = [
    "npm ci --legacy-peer-deps",
    "python3 -m venv .venv",
    ".venv/bin/pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = ". .venv/bin/activate && node server.js"
